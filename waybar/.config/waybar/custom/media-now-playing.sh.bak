#!/usr/bin/env bash

if ! playerctl -l 2>/dev/null | grep -q .; then
    echo "󰝛"
    exit 0
fi

zscroll -l 24 \
    --delay 0.3 \
    --update-check true \
    "playerctl metadata --format '{{title}} - {{artist}}'" 2>/dev/null

