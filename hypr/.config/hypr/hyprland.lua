---@module 'hl'

--- HYPRLAND CONFIGURATION ---

local keybindings = require("configs.keybindings")

local monitors = require("configs.monitors")

local autostart = require("configs.autostart")

local input = require("configs.input")

local ENV = require("configs.ENV")

local settings = require("configs.settings")

local visual = require("configs.visual")

-- HYPRMOD --
local hyprmod_hyprland_gui = require("hyprland-gui")
